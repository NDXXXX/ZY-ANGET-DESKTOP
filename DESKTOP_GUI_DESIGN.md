# Pi Desktop GUI 设计文档

> 当前版本：2026-09-17 UI 审视版。本文前面关于固定右侧工作区的内容属于历史方案；本版本以第 13 节“当前 UI 审视与新方向”为准。多对话与持久化以 [MULTI_CONVERSATION_STORAGE_DESIGN.md](./MULTI_CONVERSATION_STORAGE_DESIGN.md) 为准。

## 1. 项目目标

将现有的 Pi 终端 AI 编程助手扩展为 macOS 优先的桌面 GUI 应用。第一版的核心目标是把现有功能完整、清晰地展示在图形界面中，不新增 Agent 业务能力，不把产品提前扩展成完整 IDE。

桌面版保留现有 Agent、模型接入、文件工具、命令执行和会话能力，新增类似 Codex 的对话式工作区。应用启动后即可自由对话；用户需要处理代码时，再主动选择项目目录并进入项目模式，查看文件变化、命令执行过程和任务结果。

界面参考 Codex 的设计理念，包括克制的视觉层级、以会话为中心的工作流、渐进展示工具细节，以及任务状态始终可见。只参考理念和交互结构，使用本项目自己的名称、图标、颜色和组件，不复制第三方品牌资源或逐像素还原界面。

## 2. 设计原则

- 复用现有核心，不重写 Agent。
- 先完成现有终端功能的 GUI 映射，再增加新功能。
- 第一版不改变现有 Agent 的行为、会话格式和模型调用方式。
- 首先保证对话、工具调用和代码修改可靠，再扩展视觉功能。
- 所有可能修改文件或执行命令的操作都可见、可确认、可中止。
- 默认界面保持简单，复杂信息通过折叠面板展示。
- 支持中文界面，但代码、命令和错误信息保持原文。
- 桌面端和终端端共享同一个 Agent 能力层。

### 2.1 第一版范围

第一版只负责给现有能力提供桌面入口和可视化反馈：

- 启动后无需打开项目即可自由聊天
- 打开并信任项目目录
- 登录模型供应商、选择模型和思考级别
- 发送消息、接收流式回复和中止任务
- 展示 `read`、`write`、`edit`、`bash`、`grep`、`find`、`ls` 等现有工具调用
- 展示命令输出、错误、Token 使用量和任务状态
- 新建、恢复、命名、分支和压缩现有会话
- 展示和使用现有 Skills、Prompts、Extensions 与设置
- 复用现有认证信息、会话文件和项目上下文文件

第一版不新增完整文件编辑器、独立终端、Git 操作面板、多 Agent、云同步或新的 Agent 工具。这些属于桌面壳稳定后的功能扩展。

## 3. 目标用户流程

```text
启动应用
  ↓
直接自由对话（不启用文件和命令工具）
  ↓
需要处理代码时，选择并信任项目目录
  ↓
进入项目模式并输入任务
  ↓
Agent 流式回复并调用工具
  ↓
用户查看工具调用、Diff 和命令输出
  ↓
批准、拒绝或中止操作
  ↓
任务完成并保存会话
```

典型任务：

- 解释项目结构
- 查找并修复 Bug
- 创建新功能
- 修改多个文件
- 运行测试和构建
- 查看代码 Diff
- 总结本次修改

## 4. 第一版界面结构

### 4.1 整体布局

```text
┌─────────────────────────────────────────────────────────────┐
│ 项目名称       当前分支       模型选择       设置 主题 分享 │
├───────────────┬─────────────────────────────┬───────────────┤
│               │                             │               │
│ 会话列表      │        AI 对话区域           │ 工作区面板    │
│               │                             │               │
│ 新建会话      │ 用户消息                    │ 文件变化      │
│ 最近会话      │ Agent 回复                   │ Diff          │
│ 搜索会话      │ 工具调用                    │ 命令输出      │
│               │                             │ 任务状态      │
│ 项目列表      │                             │               │
├───────────────┴─────────────────────────────┴───────────────┤
│ 文件引用 / 输入任务                         发送 / 中止      │
└─────────────────────────────────────────────────────────────┘
```

### 4.2 左侧栏

- 新建会话按钮
- 当前项目名称和路径
- 会话搜索
- 最近会话列表
- 会话状态：进行中、已完成、失败
- 项目切换入口
- 底部显示版本、模型和连接状态

左侧栏默认宽度约 260px，可折叠。

### 4.3 顶部栏

- 当前项目名称
- 当前工作目录和 Git 分支，与现有终端底栏信息一致
- 当前模型，例如 `deepseek-v4-pro`
- 思考级别
- 工作权限模式
- 设置按钮
- 主题按钮

### 4.4 中央对话区

- 用户消息和 Agent 消息分离显示
- 支持 Markdown、代码块、表格和文件引用
- Agent 回复支持流式渲染
- 工具调用默认折叠，显示工具名称、参数摘要和执行状态
- 错误消息显示具体原因和下一步建议
- 支持停止当前任务
- 支持继续、重试和从当前消息创建分支

### 4.5 右侧工作区面板

右侧面板可以在“任务”、“文件变化”和“命令输出”之间切换。

- 任务：显示当前 Agent 步骤和整体进度
- 文件变化：显示新增、修改、删除文件数量
- Diff：显示具体文件的修改前后内容
- 命令输出：显示测试、构建和 Shell 命令输出
- 权限请求：显示等待用户批准的操作

右侧面板默认宽度约 360px，可隐藏。

### 4.6 底部输入框

- 支持多行输入
- 输入框左侧使用紧凑的 `+` 按钮选择文件，不在欢迎页放置大型项目按钮
- 已选文件在输入框上方显示为可移除的附件标签
- 支持拖拽文件
- 支持 `@` 引用项目文件
- 支持图片附件
- 支持常用提示词模板
- 发送按钮
- Agent 工作中显示中止按钮
- Enter 发送，Shift+Enter 换行

## 5. 早晚自动换色

### 5.1 模式

提供三种模式：

- 自动：根据本机时间切换
- 浅色：始终使用白天主题
- 深色：始终使用夜间主题

### 5.2 默认时间规则

```text
07:00 - 18:00  白天主题
18:00 - 07:00  夜间主题
```

时间使用系统本地时区。后续可以允许用户自定义切换时间。

### 5.3 白天主题

- 页面背景：暖白或浅灰
- 面板背景：白色
- 主文字：深灰
- 次要文字：中灰
- 强调色：蓝色
- 成功状态：绿色
- 警告状态：橙色
- 错误状态：红色

### 5.4 夜间主题

- 页面背景：接近黑色的深灰
- 面板背景：深灰
- 主文字：浅灰
- 次要文字：灰色
- 强调色：浅蓝
- 边框使用低对比度颜色
- 代码区域使用更深背景

### 5.5 主题切换行为

- 切换主题不刷新页面，不中断 Agent 任务。
- 自动模式在进入新时间段时切换。
- 手动选择浅色或深色后，不再自动切换。
- 设置保存在本地，下次启动恢复。
- 主题切换支持系统级动画，但动画时长不超过 200ms。

## 6. 技术架构

### 6.1 推荐技术栈

- 桌面容器：Electron
- 前端界面：React + TypeScript
- 前端构建：Vite
- 代码和 Diff 展示：轻量只读组件；Monaco Editor 留到后续 IDE 功能阶段
- 状态管理：React 状态或轻量状态模块
- Agent 通信：优先使用现有 RPC；必要时使用现有 SDK
- 数据存储：多对话采用 SQLite，现有 JSONL 只做历史导入；具体方案见 `MULTI_CONVERSATION_STORAGE_DESIGN.md`
- 样式：CSS 变量和组件级样式

选择 Electron 的原因：现有项目已经是 Node.js + TypeScript，Electron 主进程可以直接启动或连接现有 Agent，初期集成成本最低。

### 6.2 分层关系

```text
┌─────────────────────────────┐
│ Electron Renderer            │
│ React GUI                    │
└──────────────┬──────────────┘
               │ 安全 IPC
┌──────────────▼──────────────┐
│ Electron Main                │
│ 项目、窗口、权限、进程管理    │
└──────────────┬──────────────┘
               │ RPC / SDK
┌──────────────▼──────────────┐
│ 现有 Pi coding-agent         │
│ 会话、工具、扩展、模型选择    │
└──────────────┬──────────────┘
               │
┌──────────────▼──────────────┐
│ pi-agent-core + pi-ai        │
│ Agent 循环和模型供应商        │
└─────────────────────────────┘
```

### 6.3 Agent 通信方式

第一版优先使用 `pi --mode rpc`：

1. Electron 主进程启动 Agent 子进程。
2. 主进程通过 stdin 发送 JSON 命令。
3. Agent 通过 stdout 返回响应和流式事件。
4. 主进程把经过校验的事件转发给 Renderer。
5. Renderer 只负责显示状态，不直接执行 Shell 或访问任意文件。

RPC 事件至少需要支持：

- 用户消息
- Agent 文本增量
- 工具开始
- 工具进度
- 工具结束
- 文件变化
- 权限请求
- 错误
- 会话结束

后续可以改用 `AgentSession` SDK，减少子进程和 JSON 转换，但不作为第一阶段要求。

## 7. 建议新增目录

```text
apps/
└── desktop/
    ├── package.json
    ├── electron.vite.config.ts
    ├── src/
    │   ├── main/
    │   │   ├── main.ts             Electron 主进程入口
    │   │   ├── agent-process.ts    启动和管理 Agent
    │   │   ├── project-service.ts  项目目录和文件操作
    │   │   ├── permission-service.ts 权限确认
    │   │   └── window.ts           窗口创建
    │   ├── preload/
    │   │   └── bridge.ts           暴露白名单 IPC API
    │   ├── renderer/
    │   │   ├── App.tsx             应用入口
    │   │   ├── components/         界面组件
    │   │   ├── features/           对话、会话、Diff、项目功能
    │   │   ├── hooks/              React hooks
    │   │   ├── stores/             界面状态
    │   │   ├── styles/             主题和全局样式
    │   │   └── main.tsx            Renderer 入口
    │   └── shared/
    │       ├── ipc-types.ts        主进程和界面通信类型
    │       ├── agent-events.ts     Agent 事件类型
    │       └── settings.ts         桌面设置类型
    └── assets/
```

根目录需要把 `apps/*` 加入 npm workspaces，但不修改现有 `packages/*` 的职责。

## 8. 权限和安全设计

现有 Agent 可以读取文件、修改文件和执行命令。桌面版必须明确区分以下操作：

### 自动允许

- 读取当前工作区内的文件
- 搜索当前工作区
- 查看 Git 状态

### 默认需要确认

- 写入或删除文件
- 执行 Shell 命令
- 安装依赖
- 访问工作区外路径
- 访问网络或上传文件
- 修改 Git 历史

### 安全要求

- Renderer 不启用 Node.js 直接访问能力。
- 所有文件和命令操作由 Main 进程执行。
- IPC 使用白名单方法，不允许 Renderer 发送任意 Electron 消息。
- 权限弹窗显示完整命令、工作目录和风险提示。
- 用户可以选择“本次允许”“本次拒绝”“本项目始终允许”。
- API Key 不进入聊天记录，不显示在日志和 Diff 中。
- 退出应用时中止正在运行的 Agent 进程。

## 9. 开发阶段

### 阶段一：桌面壳和基础对话

- 创建 Electron 应用窗口
- 启动后直接进入不带工具的自由对话模式
- 打开项目目录
- 启动现有 Agent RPC
- 输入消息并显示流式回复
- 支持 DeepSeek
- 支持浅色、深色和自动主题

验收标准：无需选择项目即可自由聊天；打开项目后，可以对当前项目提问并看到 DeepSeek 的完整回复。

### 阶段二：现有工具可视化

- 显示现有 read、edit、write、bash、grep、find、ls 工具调用
- 增加命令和文件修改确认
- 支持停止任务
- 显示执行错误
- 显示工具输入、进度、结果和耗时

验收标准：Agent 修改文件或运行命令前，用户能看到并决定是否允许。

### 阶段三：现有会话和资源功能映射

- 新建、恢复、命名、分支和压缩会话
- 显示会话 Token、成本和上下文占用
- 映射模型、思考级别和设置选择器
- 展示 Skills、Prompts、Extensions 和上下文文件
- 显示文件修改 Diff 和任务结果摘要

验收标准：不依赖终端命令，用户可以在 GUI 中完成现有 Pi 的主要日常工作流。

### 阶段四：兼容性、恢复和打包

- 与终端版共享认证、设置和会话
- Agent 子进程崩溃恢复
- 多项目切换和最近项目列表
- macOS `.dmg` 或 `.zip`
- 应用图标和名称
- 自动更新预留
- 崩溃日志和诊断信息
- Windows、Linux 构建预留

验收标准：关闭应用后重新打开可以恢复项目和历史会话，桌面版与终端版的数据保持兼容。

### 后续阶段：新增桌面能力

- 文件树和文件搜索
- Monaco Editor
- 独立终端
- Git 操作面板
- 更完整的 Diff 审查工作流

这些功能不属于第一版现有功能 GUI 化的验收范围。

## 10. 暂不做的功能

第一版暂不加入：

- 多 Agent 协作
- 云端账号系统
- 在线项目同步
- 团队权限管理
- 复杂插件市场
- 内置完整 IDE 调试器
- 文件树和完整代码编辑器
- 独立终端和 Git 操作面板
- 自动提交和推送 Git

这些功能会显著增加权限、状态同步和产品复杂度，应等单机版稳定后再考虑。

## 11. 第一版完成标准

当以下流程全部可用时，桌面 GUI MVP 完成：

```text
打开应用
  ↓
选择项目目录
  ↓
使用 DeepSeek 发起对话
  ↓
Agent 读取项目文件
  ↓
用户批准一次文件修改或命令执行
  ↓
界面显示流式回复和工具日志
  ↓
显示修改后的 Diff
  ↓
保存并恢复会话
```

## 12. 结论

桌面 GUI 应该作为现有 Pi 项目的新应用层，而不是修改 `pi-agent-core` 或 `pi-ai` 的内部实现。

最小实现路径是：

```text
Electron + React
  → 安全 IPC
  → 现有 RPC Agent
  → DeepSeek / 其他模型
```

第一版以“现有能力的 GUI 化”为唯一产品边界。完成现有功能映射后，再根据真实使用体验增加文件树、编辑器、Git 等桌面专属能力。

## 13. 当前 UI 审视与新方向

### 13.1 本次已确定的布局决策

右侧运行记录栏已经移除。它在空状态下长期占据固定空间，只显示“还没有工具记录”，导致主对话区变窄，也让用户误以为应用需要同时操作两个工作区。

当前主布局改为单栏：

```text
┌─────────────────────────────────────────┐
│ 当前项目 / 模型 / 轻量操作                │
├─────────────────────────────────────────┤
│                                         │
│              对话内容                    │
│                                         │
├─────────────────────────────────────────┤
│ 附件 / 输入任务 / 发送或停止              │
└─────────────────────────────────────────┘
```

工具调用仍然由 Agent 产生，但不再在固定右栏展示。后续如需让用户查看工具细节，应以内嵌消息、临时抽屉或按需展开的方式呈现，并贴近触发它的对话内容。

顶部右侧的模型标签和“打开项目”按钮也已移除。模型信息属于配置层信息，不应在每次进入对话时占据注意力；打开项目已经由侧边栏“项目”入口承担，保留一个入口即可。

空会话时的 Pi 标志、欢迎标题、说明文字和快捷键提示同样已移除。它们只是重复解释“可以聊天”，却把视觉重心放在静态欢迎卡片上。空状态保持留白，输入框成为唯一明确的下一步。

当前进一步移除主区域顶部的项目标题、自由对话说明和模型信息。项目入口已经在侧边栏，模型属于配置状态，不应持续占据对话区域。

消息展示采用更接近 ChatGPT 的对话结构：

- Agent 回复直接从主内容左侧开始，不显示 Pi 头像、角色标签或竖向时间线。
- 用户消息靠右显示为蓝色气泡。
- 对话列表不整体居中，只限制单条正文的最大阅读宽度。
- 空会话不放欢迎卡片，让输入框成为唯一主要动作。

夜间主题的页面、侧边栏和主对话背景使用纯黑 `#000000`；输入框等交互表面保留深灰层次，用于区分可编辑区域。

### 13.2 当前不合理之处

#### 1. 导航入口有视觉呈现但没有真实功能

“图像”“定时任务”“插件”“探索”目前是无点击行为的按钮。它们会被辅助技术识别为可操作控件，却不会产生结果，容易造成错误预期。

处理原则：功能完成前改为明确的禁用态，或者暂时不展示；不要长期保留“看起来能点、实际上没反应”的控件。

#### 2. “置顶”与实际数据不一致

当前实现只有“当前会话”，但侧边栏使用了“置顶”分组名称。真实会话列表、置顶和最近会话尚未接入，因此这个标题表达了不存在的能力。

在会话数据接入前，建议使用“当前会话”；接入后再按真实数据展示“置顶”和“最近”。

#### 3. 打开项目入口重复

侧边栏和顶部栏都能打开项目。两个入口的行为相同，却占据两个视觉位置。

建议长期保留侧边栏项目入口；顶部栏只显示当前项目名称和路径。无项目时，顶部栏可以暂时显示一次轻量的打开项目动作。

#### 4. 消息仍是纯文本渲染

当前消息使用 `white-space: pre-wrap`，Markdown、代码块、表格、文件路径和链接没有独立的视觉层级。对于编程助手，这会降低扫描、复制和定位信息的效率。

下一步应加入只读 Markdown 渲染，至少覆盖标题、列表、代码块、复制、文件路径、行号、链接和错误信息；暂不引入完整编辑器。

#### 5. 主题颜色存在硬编码

部分图标直接使用固定深灰色，在深色主题中可能失去对比度。图标、边框和状态颜色应使用语义变量，而不是直接写颜色值。

建议统一使用 `--text`、`--muted`、`--faint`、`--line`、`--accent`、`--success` 和 `--danger` 等 Token。

#### 6. 响应式规则与窗口限制冲突

`body` 的 `min-width: 900px` 会阻止窄窗口真正收缩；现有断点因此只能隐藏部分内容，不能形成完整的响应式策略。

建议由 Electron 窗口配置负责必要的最小尺寸，CSS 负责内容伸缩。主区域使用 `minmax(0, 1fr)`，顶部次要信息在窄窗口隐藏，输入框始终不产生横向溢出。

#### 7. 动态状态缺少辅助技术提示

流式回复、连接失败和 Agent 停止目前主要依靠视觉变化表达。后续应为回复区域增加 `aria-live="polite"`，为错误区域增加 `aria-live="assertive"`，并给文本输入框提供显式可访问名称。

### 13.3 视觉规范

#### 侧边栏

- 默认宽度控制在约 240–270px。
- 侧边栏与主内容之间使用可拖拽分隔条，允许用户按工作习惯调整宽度。
- 分隔条限制在 220–420px，并通过本地设置记住用户的宽度。
- 分隔条支持键盘左右方向键，Home 回到最窄，End 调到最宽。
- 使用图标加文字的轻量列表，不使用厚边框、阴影和重复卡片。
- 选中态只使用低对比度浅色背景。
- 导航文字建议 14px；分组标题 12–13px；辅助信息 11–12px。
- 分组之间使用留白，不依赖大量分隔线。

#### 主对话区

- 使用单栏，内容设置最大阅读宽度，避免超长行。
- 用户消息和 Agent 消息有明确层级，但避免过重的气泡和装饰线。
- 主标题控制在 16–18px，正文约 14px，正文行高 1.6–1.75。
- 输入框横向使用主区域全部可用空间，不为隐藏面板预留空白。

#### 控件

- 语义上可以使用 `<button>`，但视觉上不做成大块按钮。
- 只有主要动作使用填充背景，次要动作使用文字或 ghost 样式。
- 仅图标控件必须有稳定的 `aria-label` 和可见焦点态。
- 禁用状态必须明确，不允许只是点击无反应。

### 13.4 推荐实现顺序

1. 完成单栏布局并验证不同窗口宽度下的对话和输入框。
2. 修复无行为导航入口和重复的打开项目入口。
3. 接入真实会话数据，再决定“置顶”和“最近”的分组。
4. 增加 Markdown、代码块和文件引用渲染。
5. 补齐流式回复、错误和表单的无障碍提示。
6. 根据真实使用反馈，决定工具详情使用内嵌消息还是临时抽屉。

### 13.5 技术边界

当前项目继续使用 Electron、React、TypeScript 和原生 CSS。`ui-styling` 技能提供的是视觉层级、设计 Token、响应式和无障碍方法，不要求为了“使用技能”而迁移到 Tailwind、shadcn/ui 或 Radix。

本轮验收标准：

- 右侧运行记录栏完全不存在。
- 主内容和输入框不再为右侧栏预留空间。
- 现有 Agent、流式回复、项目、附件、停止和主题功能不受影响。
- 侧边栏保持紧凑的轻量列表风格。
- TypeScript 和项目检查通过。
